{
  "private": true,
  "name": "add-ooo-to-services-google-calendar",
  "main": "index.js",
  "scripts": {
    "start": "node ./index.js",
    "lint": "prettier --single-quote --no-semi --trailing-comma none --quote-props consistent --write \"**/*.js\"",
    "test": "npm run lint && jest"
  },
  "dependencies": {
    "actions-toolkit": "^2.0.0",
    "googleapis": "^39.2.0"
  },
  "devDependencies": {
    "jest": "^24.8.0",
    "prettier": "^1.17.1"
  }
}
