{
  "private": true,
  "name": "add-ooo-to-services-google-calendar",
  "main": "index.js",
  "scripts": {
    "start": "node ./index.js",
    "lint": "prettier --print-width 100 --single-quote --no-semi --trailing-comma none --quote-props consistent --write \"**/*.js\"",
    "test": "npm run lint && jest"
  },
  "dependencies": {
    "actions-toolkit": "^2.0.0",
    "googleapis": "^39.2.0",
    "sherlockjs": "^1.4.0"
  },
  "devDependencies": {
    "jest": "^24.8.0",
    "mockdate": "^2.0.2",
    "prettier": "^1.17.1"
  }
}
